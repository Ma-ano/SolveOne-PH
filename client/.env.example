# Public build-time configuration only. Never place secrets in EXPO_PUBLIC_* values.
# Replace localhost with a reachable host when testing on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:5000/api/v1
EXPO_PUBLIC_WEB_URL=http://localhost:8081
